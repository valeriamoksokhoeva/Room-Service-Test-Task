version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused

exclusions:
  rules:
    - path: _test\.go
      linters:
        - errcheck
    - path: internal/tests/
      linters:
        - errcheck